apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: nibas-frontend
  namespace: nibas
spec:
  selector:
    istio: ingressgateway # only internal ingress
    ingress: internal
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "nibas.dev.skip.statkart.no"