apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: baat-geonorge-no
  namespace: nibas
spec:
  hosts:
    - baat.geonorge.no
  gateways:
    - istio-system/baat-geonorge-no
  http:
  - match:
    - gateways:
      - mesh
      port: 80
    route:
    - destination:
        host: baat-geonorge-no.istio-system.svc.cluster.local
        subset: target-egress-gateway
        port:
          number: 80
  - match:
      - port: 443
    route:
      - destination:
          host: baat.geonorge.no
          port:
            number: 443
