/**
 * Hjelpefunksjon for å sikre at et parameter ikke lenger kan ha en verdi.
 * Sikrer at parameterets type har vært gjennom et switch case og alle caser dekker typen til parameteret
 * @param _ Parameteret som skal ha vært sjekket
 * @returns
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const ensureAllCasesCovered = (_: never) => undefined;
